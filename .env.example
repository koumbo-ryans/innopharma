# Example environment variables for Docker deployment
PORT=5000
DB_HOST=db
DB_USER=innopharma
DB_PASSWORD=change_me
DB_NAME=innopharma_db
MYSQL_ROOT_PASSWORD=change_me_root

# When you run locally, create a .env file with secure passwords.
# Environment variables for innopharma
# Copy this file to .env and fill in your actual values

# JWT Configuration
JWT_SECRET=your_super_secure_random_jwt_secret_here

# Database Configuration
DB_HOST=localhost
DB_USER=your_db_username
DB_PASSWORD=your_db_password
DB_NAME=innopharma_db

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS in production)
FRONTEND_URL=https://yourdomain.com